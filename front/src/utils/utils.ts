//this is a file that will include functions that we might want to use later in several files.

export type FormMyInfo = {
    fname: string;
    lname: string;
    phone: string;
    email: string;
    msg: string;
}